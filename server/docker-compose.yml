version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: snapasset-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 3

  api:
    build: .
    container_name: snapasset-api
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - JWT_SECRET=${JWT_SECRET:-change-me-to-a-random-string-at-least-32-characters}
      - DB_PROVIDER=${DB_PROVIDER:-local}
      - LOCAL_DATA_DIR=/usr/src/app/data
    env_file:
      - .env
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped
    volumes:
      - ./logs:/usr/src/app/logs
      - app_data:/usr/src/app/data

  worker:
    build: .
    command: node workers/imageWorker.js
    environment:
      - NODE_ENV=production
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - JWT_SECRET=${JWT_SECRET:-change-me-to-a-random-string-at-least-32-characters}
      - DB_PROVIDER=${DB_PROVIDER:-local}
      - LOCAL_DATA_DIR=/usr/src/app/data
    env_file:
      - .env
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped
    volumes:
      - app_data:/usr/src/app/data
    deploy:
      replicas: 2  # Run 2 worker instances

volumes:
  redis_data:
  app_data: